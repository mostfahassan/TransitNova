@model object?
@{
    ViewData["Title"] = "Roles";
    var header = new AdminPageHeaderViewModel
    {
        Title = "Roles",
        Description = "Control the authorization surface used by MVC and API policies.",
        PrimaryLabel = "Create role",
        PrimaryController = "Roles",
        PrimaryAction = "Create"
    };
    var table = new AdminTableViewModel
    {
        Title = "Role registry",
        Subtitle = "Application roles available to the identity system.",
        Source = Model,
        Controller = "Roles",
        RouteIdName = "roleId",
        IdPropertyPath = "RoleId|Id",
        ShowDetails = true,
        ShowEdit = true,
        ShowDelete = true,
        RowActions = new List<AdminRowActionViewModel>
        {
            new("RoleMembers", "RoleMembers")
        },
        Columns = new List<AdminTableColumnViewModel>
        {
            new("Role", "RoleName|Name"),
            new("Total Users", "TotalUsers|UsersCount")
        }
    };
}

<partial name="_AdminPageHeader" model="header" />
<partial name="_AdminTable" model="table" />

@model UiRoleSummaryDto?
@{
    ViewData["Title"] = "Role details";
    var role = Model ?? new UiRoleSummaryDto();
    var header = new AdminPageHeaderViewModel
    {
        Title = "Role details",
        Description = "Inspect the selected role before editing its name or reviewing its members.",
        SecondaryLabel = "Back",
        SecondaryController = "Roles",
        SecondaryAction = "Index"
    };
    var detail = new AdminDetailViewModel
    {
        Title = "Role record",
        Subtitle = "Authorization role summary from the backend.",
        PrimaryLabel = role.RoleName ?? string.Empty,
        Controller = "Roles",
        EditAction = "Edit",
        DeleteAction = "Delete",
        RouteValues = new Dictionary<string, object?> { ["roleId"] = role.RoleId },
        Fields =
        [
            AdminDisplay.AdminCell("Role", role.RoleName),
            AdminDisplay.AdminCell("Total Users", role.TotalUsers)
        ]
    };
}

<partial name="_AdminPageHeader" model="header" />
<partial name="_AdminDetail" model="detail" />
